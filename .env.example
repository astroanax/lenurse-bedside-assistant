# OpenRouter API Configuration
# Get your API key from: https://openrouter.ai/keys
OPENROUTER_API_KEY=your_openrouter_api_key_here

# VLA Model Configuration
# Device for VLA model inference (cpu or cuda)
VLA_DEVICE=cpu

# HuggingFace Configuration (Optional - for VLA model downloads)
# Get token from: https://huggingface.co/settings/tokens
# HF_TOKEN=your_huggingface_token_here

# Google Cloud Configuration (Optional - for voice features)
# If not set, browser-based speech will be used instead
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/google-cloud-credentials.json

# Serial Port Configuration (Optional)
# Uncomment and modify if your servo controller uses a different port
# SERVO_PORT=/dev/ttyACM0

# Camera Configuration (Optional)
# Default camera device ID (will auto-detect if not set)
# CAMERA_DEVICE_ID=0
